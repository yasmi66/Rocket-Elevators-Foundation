<p id="notice"><%= notice %></p>

<h1>Batteries</h1>

<table>
  <thead>
    <tr>
      <th>Building</th>
      <th>Employee</th>
      <th>Type</th>
      <th>Status</th>
      <th>Date commissioning</th>
      <th>Date last inspection</th>
      <th>Certificate operations</th>
      <th>Information</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @batteries.each do |battery| %>
      <tr>
        <td><%= battery.building %></td>
        <td><%= battery.employee %></td>
        <td><%= battery.type %></td>
        <td><%= battery.status %></td>
        <td><%= battery.date_commissioning %></td>
        <td><%= battery.date_last_inspection %></td>
        <td><%= battery.certificate_operations %></td>
        <td><%= battery.information %></td>
        <td><%= battery.notes %></td>
        <td><%= link_to 'Show', battery %></td>
        <td><%= link_to 'Edit', edit_battery_path(battery) %></td>
        <td><%= link_to 'Destroy', battery, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Battery', new_battery_path %>
